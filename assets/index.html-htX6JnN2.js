import{_ as r}from"./plugin-vue_export-helper-DlAUqK2U.js";import{c as t,a,o as s}from"./app-D9ZsBuuN.js";const n={};function i(o,e){return s(),t("div",null,[...e[0]||(e[0]=[a(`<h1 id="elasticsearch-架构" tabindex="-1"><a class="header-anchor" href="#elasticsearch-架构"><span>Elasticsearch 架构</span></a></h1><h2 id="存储流程" tabindex="-1"><a class="header-anchor" href="#存储流程"><span>存储流程</span></a></h2><p>ES 存储数据的流程可以从三个角度来阐述：</p><ul><li>从<strong>集群</strong>的角度来看，数据写入会先路由到主分片，在主分片上写入成功后，会并发写副本分片，最后响应给客户端。</li><li>从<strong>分片</strong>的角度来看，数据到达分片后需要对内容进行格式校验、分词处理然后再索引数据。</li><li>从<strong>节点</strong>的角度来看，ES 数据持久化的步骤可归纳为：<strong>Refresh、写 Translog、Flush、Merge。</strong></li></ul><h3 id="文档分布式存储流程" tabindex="-1"><a class="header-anchor" href="#文档分布式存储流程"><span>文档分布式存储流程</span></a></h3><p>ES 的索引有一个或者多个分片，而分片又分为主分片和副本分片两种。将要写入的数据存储在哪个分片是第一个要考虑的问题。</p><p>首先需要找到存储文档的主分片，并在主分片的节点上写入对应数据，<strong>数据在主分片写入成功后再将数据分发到副分片进行存储</strong>。文档的新增、更新、删除等操作都属于写入操作。</p><p>从集群层面来看，存储数据的流程如下：</p><ol><li><strong>请求</strong> - 客户端选择一个 node（示例中是 node1）发送请求过去，这个 node 就是 <code>coordinating node</code>（协调节点）。</li><li><strong>路由转发</strong> - <code>coordinating node</code> 根据文档 ID 或 routing key 计算出文档应该被保存到哪个分片（这里是分片 3），并且从集群状态的路由表信息中获取分片 3 的主分片所在的节点为 node3。<code>coordinating node</code> 将请求转发给 node3。</li><li><strong>复制</strong> - node3 存储数据后，将请求并发转发到 分片 3 的所有副本分片，即数据复制。</li><li><strong>响应</strong> - 当所有副分片都写入成功后，node3 会向 <code>coordinating node</code> 返回写入成功的消息，<code>coordinating node</code> 再将响应返回给客户端。</li></ol><h3 id="数据索引流程" tabindex="-1"><a class="header-anchor" href="#数据索引流程"><span>数据索引流程</span></a></h3><p>文档分布式存储流程中的描述，隐藏了一个细节：如果是全文本数据，ES 需要使用 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/analyzer-anatomy.html" target="_blank" rel="noopener noreferrer"><strong>analyzer（分析器）</strong></a> 先对内容进行分析（如果数据是精确值，如实体 ID、日期等，则无需处理）。</p><p>在 Elasticsearch 中，分析器是用于对文本进行分词的组件。分析器用于将文本分解为更小的单元，称为分词。然后，这些分词用于索引和搜索文本。分析器的主要目标是将原始文本转换为可以有效搜索和分析的结构化格式 （分词）。</p><p><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/analyzer-anatomy.html" target="_blank" rel="noopener noreferrer"><strong>analyzer（分析器）</strong></a> 由三个组件组成：零个或多个 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/analysis-charfilters.html" target="_blank" rel="noopener noreferrer">Character Filters（字符过滤器）</a>、有且仅有一个 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/analysis-tokenizers.html" target="_blank" rel="noopener noreferrer">Tokenizer（分词器）</a>、零个或多个 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/analysis-tokenfilters.html" target="_blank" rel="noopener noreferrer">Token Filters（分词过滤器）</a>。分析的执行顺序为：<code>character filters -&gt; tokenizer -&gt; token filters</code>。</p><p>对全文本数据来说，数据索引时会对文本内容进行分析处理，分析器的处理流程如下：</p><ol><li>character flters 先对字符进行过滤，例如：把一些 HTML 元素、转义标签清除；</li><li>tokenizer 会将字符串按不同的策略进行切分，分割得到的单词称为 token（词条）；</li><li>token filters 对 token 再进行过滤，例如：删除停用词（and、is 等），转换近义词等；</li></ol><p>经过以上一系列处理后，ES 会将数据存储到名为倒排索引的结构中。</p><p>当需要全文检索存储数据时，需要先使用搜索分析器对搜索内容进行分析，这个处理过程和存储时使用的分析器相似。通过分析得到的分词列表，再去和倒排索引中的数据去进行匹配，最后返回匹配度最高的数据。</p><h3 id="数据持久化流程" tabindex="-1"><a class="header-anchor" href="#数据持久化流程"><span>数据持久化流程</span></a></h3><p>ES 的数据持久化流程主要有以下几个过程：<strong>Refresh、写 Translog、Flush、Merge。</strong></p><figure><img src="https://raw.githubusercontent.com/dunwu/images/master/archive/2025/03/17e5ffbe2dad440d981823db4e9d4be5.png" alt="" tabindex="0" loading="lazy"><figcaption></figcaption></figure><h4 id="refresh" tabindex="-1"><a class="header-anchor" href="#refresh"><span>Refresh</span></a></h4><p>在文档写入的时候，ES 会将文档先写入到 <strong>Index Buffer</strong> 中。</p><p>当 Index Buffer 大小达到阈值（默认为 JVM 的 10%），或间隔一段时间（默认每秒执行一次，可以通过 <code>index.refresh_interval</code> 进行设置），ES 会将 Index Buffer 中的数据写入到一个新的 Segment 文件中。此时的 Segment 文件存在于 OS Cache 中。这个过程称为 <strong>Refresh</strong>。</p><p>refresh 写完 segment 后，会更新 shard 的 commit point。commit point 在 shard 中以 <code>segments_xxx</code> 形式名字的文件存在，用来记录每个 shard 中 segment 相关的信息。</p><p>此外，ES 也支持通过 API 手动触发 Refresh 操作。</p><p>Refresh 过程有几点需要注意：</p><ul><li>在 Index Buffer 中的数据是搜索不到的；Refresh 后，数据进入 <strong>OS Cache</strong>，这时数据就可以搜索了。由于，刷新默认间隔一秒，写入的数据需要一秒后才可见，因此，ES 被称为近实时搜索数据库。</li><li>Index Buffer 的设计是为了通过批量写入，提高写入效率。但是，这种设计也带来了新的问题：一旦 ES 节点发生断点，Index Buffer 中的数据就丢失了。为了避免数据丢失，ES 的解决方案就是下文要提到的 <strong>Translog</strong>。</li><li>Index Buffer 每次 Refresh 时，都会创建一个新的 Segment 文件。随着时间推移，Segment 文件会越来越多。这些 Segment 都要消耗文件句柄和内存，每次搜索都要检查每个 Segment 然后再合并结果。因此，Segment 越多、搜索也越慢。为了减少 Segment 文件数，ES 的解决方案就是下文要提到的 <strong>Merge</strong> 操作。</li></ul><h4 id="写-translog" tabindex="-1"><a class="header-anchor" href="#写-translog"><span>写 Translog</span></a></h4><p><strong>ES 通过 Translog（事务日志）来保证数据不丢失</strong>。</p><p>数据写入 Index Buffer 后，ES 会将数据也写入 Translog，写入完毕后即可以返回客户端写入成功。<strong>Translog 只允许追加写入</strong>，并且默认是调用 fsync 进行刷盘的。<strong>每个分片都会有自己的 Translog，在 Refresh 的时候系统会清空 Index Buffer，但不会清空 Translog</strong>。一旦机器宕机，再次重启的时候， ES 会自动读取 Translog 中的数据，恢复到 Index Buffer 和 OS Cache 中。</p><p>Translog 其实也是先写入 OS Cache 的，默认每 5 秒刷一次到磁盘中去（由 <code>index.translog.interval</code> 控制）。所以，如果机器宕机，可能会丢失 5 秒的数据。这样设计的目的，还是基于写入效率的考虑。如果每条数据都直接写入磁盘，开销是比较高的，所以这里设计为延时批量写入。</p><blockquote><p>通过 Refresh 和 写 Translog 两节的内容，我们可以总结为：</p><ul><li>ES 之所以被称为<strong>近实时查询</strong>，是由于数据写入后，需要刷新（默认间隔 1 秒）后，才可以搜索到；</li><li>ES 虽然有 Translog 机制，但依然有丢失数据的风险——有 5 秒的数据，是暂存在 index buffer、translog(os cache)、segment file(os cache) 中，此时尚未保存到磁盘。如果此时发生宕机或断电，会<strong>丢失 5 秒的数据</strong>。</li></ul></blockquote><h4 id="flush" tabindex="-1"><a class="header-anchor" href="#flush"><span>Flush</span></a></h4><p>Flush 操作本质上就是 commit 操作，即 ES 的数据持久化操作。</p><ol><li>Flush 操作的第一步，就是将 index buffer 中现有数据 <code>refresh</code> 到 <code>OS Cache</code> 中去，清空 buffer。</li><li>然后，将一个 <code>commit point</code> 写入磁盘文件，里面标识着这个 <code>commit point</code> 对应的所有 Segment 文件。同时，强行将 <code>OS Cache</code> 中目前所有的数据都 <code>fsync</code> 到磁盘中去。</li><li>最后，删除当前的 translog，新建一个 translog，此时 commit 操作完成。</li></ol><p>以下两个条件满足任意一个，就会触发 Flush 操作：</p><ul><li>默认每 30 分钟触发执行一次（由 <code>index.translog.flush_threshold_period</code> 控制）</li><li>Translog 写满时触发执行，默认容量为 512M（由 <code>index.translog.flush_threshold_size</code> 控制）。</li></ul><h4 id="merge" tabindex="-1"><a class="header-anchor" href="#merge"><span>Merge</span></a></h4><p>Elasticsearch 的 document 的物理存储是 Luncene segment，而 segment 不允许变更。那么，如何处理删除和更新呢？</p><ul><li><p>如果是删除操作，commit 的时候会生成一个 <code>.del</code> 文件，里面将某个 doc 标识为 <code>deleted</code> 状态，那么搜索的时候根据 <code>.del</code> 文件就知道这个 doc 是否被删除了。</p></li><li><p>如果是更新操作，就是将原来的 doc 标识为 <code>deleted</code> 状态，然后新写入一条数据。</p></li></ul><p>Index Buffer 每次 Refresh 时，都会创建一个新的 Segment 文件。随着时间推移，Segment 文件会越来越多。这些 Segment 都要消耗文件句柄和内存，每次搜索都要检查每个 Segment 然后再合并结果。因此，Segment 越多、搜索也越慢。</p><p>Elasticsearch 会定期执行 merge 操作，将多个 <code>segment file</code> 合并成一个。合并时会将标识为 <code>deleted</code> 的 doc 给<strong>物理删除掉</strong>，然后将新的 <code>segment file</code> 写入磁盘，这里会写一个 <code>commit point</code>，标识所有新的 <code>segment file</code>，然后打开 <code>segment file</code> 供搜索使用，同时删除旧的 <code>segment file</code>。</p><h2 id="搜索流程" tabindex="-1"><a class="header-anchor" href="#搜索流程"><span>搜索流程</span></a></h2><p>在 Elasticsearch 中，搜索一般分为两个阶段，query 和 fetch 阶段。可以简单的理解，query 阶段确定要取哪些 doc，fetch 阶段取出具体的 doc。</p><h3 id="query-阶段" tabindex="-1"><a class="header-anchor" href="#query-阶段"><span>Query 阶段</span></a></h3><p>Query 阶段会根据搜索条件遍历每个分片（主分片或者副分片中的其一）中的数据，返回符合条件的前 N 条数据的 ID 和排序值，然后在协调节点中对所有分片的数据进行排序，获取前 N 条数据的 ID。</p><p>Query 阶段的流程如下：</p><ol><li>客户端选择一个节点发送请求，这个 node 成为 coordinate node（协调节点）。coordinate node 创建一个大小为 from + size 的优先级队列用来存放结果。</li><li>coordinate node 将请求转发到索引的每个主分片或者副分片中。</li><li>每个分片在本地执行搜索请求，并将查询结果打分排序，然后将结果保存到 from + size 大小的有序队列中。</li><li>接着，每个分片将结果返回给 coordinate node，coordinate node 对数据进行汇总处理：合并、排序、分页，将汇总数据存到一个大小为 from + size 的全局有序队列。</li></ol><p>需要注意的是，在协调节点转发搜索请求的时候，如果有 N 个 Shard 位于同一个节点时，并不会合并这些请求，而是发生 N 次请求！</p><h3 id="fetch-阶段" tabindex="-1"><a class="header-anchor" href="#fetch-阶段"><span>Fetch 阶段</span></a></h3><p>在 Fetch 阶段，协调节点会从 Query 阶段产生的全局排序列表中确定需要取回的文档 ID 列表，然后通过路由算法计算出各个文档对应的分片，并且用 multi get 的方式到对应的分片上获取文档数据。</p><p>Fetch 阶段的流程如下：</p><ol><li>coordinate node 确定需要获取哪些文档，然后向相关节点发起 multi get 请求；</li><li>分片所在节点读取文档数据，并且进行 <code>_source</code> 字段过滤、处理高亮参数等，然后把处理后的文档数据返回给协调节点；</li><li>coordinate node 汇总所有数据后，返回给客户端。</li></ol><h3 id="深度分页问题" tabindex="-1"><a class="header-anchor" href="#深度分页问题"><span>深度分页问题</span></a></h3><p>在 Elasticsearch 中，支持三种分页查询方式：</p><ul><li>from + size - 可以使用 <code>from</code> 和 <code>size</code> 参数分别指定查询的起始页和每页记录数。</li><li><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/paginate-search-results.html#search-after" target="_blank" rel="noopener noreferrer"><code>search_after</code></a> - 不支持指定页数，只能向下翻页；并且需要指定 sort，并保证值是唯一的。然后，可以反复使用上次结果中最后一个文档的 sort 值进行查询。</li><li><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/paginate-search-results.html#scroll-search-results" target="_blank" rel="noopener noreferrer">scroll</a> - 类似于 RDBMS 中的游标，只允许向下翻页。每次下一页查询后，使用返回结果的 scroll id 来作为下一次翻页的标记。scroll 查询会在搜索初始化阶段会生成快照，后续数据的变化无法及时体现在查询结果，因此更加适合一次性批量查询或非实时数据的分页查询。</li></ul><p>前文中，我们已经了解了 ES 两阶段搜索流程（Query 和 Fetch）。从中不难发现，这种搜索方式在分页查询时会出现以下情况：</p><ul><li>每个 shard 要扫描 <code>from + size</code> 条数据；</li><li>coordinate node 需要接收并处理 <code>(from + size) * primary_shard_num</code> 条数据。</li></ul><p><strong>如果 from 或 size 很大，需要处理的数据量也会很大，代价很高，这就是深分页产生的原因</strong>。为了避免深分页，ES 默认限制 <code>from + size</code> 不能超过 10000，可以通过 <code>index.max_result_window</code> 设置。</p><p>如何解决 Elasticsearch 深分页问题？</p><p>ES 官方提供了另外两种分页查询方式 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/paginate-search-results.html#search-after" target="_blank" rel="noopener noreferrer"><code>search_after</code></a> + PIT 和 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/paginate-search-results.html#scroll-search-results" target="_blank" rel="noopener noreferrer">scroll</a>（注意：官方已不再推荐） 来避免深分页问题。</p><h3 id="计算偏差" tabindex="-1"><a class="header-anchor" href="#计算偏差"><span>计算偏差</span></a></h3><p>在 ES 中，不仅仅是普通搜索，相关性计算（评分）和聚合计算也是先在每个 shard 的本地进行计算，再由 coordinate node 进行汇总。由于分片的本地计算是独立的，只能基于数据子集来进行计算，所以难免出现数据偏差。</p><p>解决这个问题的方式也有多种：</p><ul><li>当数据量不大的情况下，<strong>设置主分片数为 1</strong>，这意味着在数据全集上进行聚合。 但这种方案不太现实。</li><li><strong>设置 <a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/search-aggregations-bucket-terms-aggregation.html#search-aggregations-bucket-terms-aggregation-shard-size" target="_blank" rel="noopener noreferrer"><code>shard_size</code></a> 参数</strong>，将计算数据范围变大，<strong>牺牲整体性能，提高精准度</strong>。shard_size 的默认值是 <code>size * 1.5 + 10</code>。</li><li><strong>使用 DFS Query Then Fetch</strong>， 在 URL 参数中指定：<code>_search?search_type=dfs_query_then_fetch</code>。这样设定之后，系统先会把每个分片的词频和文档频率的数据汇总到协调节点进行处理，然后再进行相关性算分。这样的话会消耗更多的 CPU 和内存资源，效率低下！</li><li>尽量保证数据均匀地分布在各个分片中。</li></ul><h3 id="数据路由" tabindex="-1"><a class="header-anchor" href="#数据路由"><span>数据路由</span></a></h3><p>为了避免出现数据倾斜，系统需要一种高效的方式把数据均匀分散到各个节点上<strong>存储</strong>，并且<strong>在检索的时候可以快速找到</strong>文档所在的节点与分片。这就需要确立路由算法，使得数据可以映射到指定的节点上。</p><p>常见的路由方式如下：</p><table><thead><tr><th style="text-align:left;"><strong>算法</strong></th><th style="text-align:left;"><strong>描述</strong></th></tr></thead><tbody><tr><td style="text-align:left;">随机算法</td><td style="text-align:left;">写数据时，随机写入到一个节点中；读数据时，由于不知道查询数据存在于哪个节点，所以需要遍历所有节点。</td></tr><tr><td style="text-align:left;">路由表</td><td style="text-align:left;">由中心节点统一维护数据的路由表，以保证唯一性；但是，中心化产生了新的问题：单点故障、数据越大，路由表越大、单点容易称为性能瓶颈、数据迁移复杂等。</td></tr><tr><td style="text-align:left;">哈希取模</td><td style="text-align:left;">对 key 值进行哈希计算，然后根据节点数取模，以确定节点。</td></tr></tbody></table><p>ES 的数据路由算法是根据文档 ID 和 routing key 来确定 Shard ID 的过程。<strong>默认的情况下 routing key 为文档 ID</strong>，路由算法一般情况下的计算公式如下：</p><div class="language- line-numbers-mode" data-highlighter="shiki" data-ext="" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-"><span class="line"><span> shard_number = hash(_routing) % numer_of_primary_shards</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>也可以在请求中指定 routing key，下面是新增数据的时候指定 routing 的方式：</p><div class="language-bash line-numbers-mode" data-highlighter="shiki" data-ext="bash" style="--shiki-light:#383A42;--shiki-dark:#abb2bf;--shiki-light-bg:#FAFAFA;--shiki-dark-bg:#282c34;"><pre class="shiki shiki-themes one-light one-dark-pro vp-code"><code class="language-bash"><span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">PUT</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;"> &lt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">inde</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">x&gt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">/_doc/</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">&lt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">i</span><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">d&gt;</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;">?routing=routing_key</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">{</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    &quot;field1&quot;</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">:</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;xxx&quot;,</span></span>
<span class="line"><span style="--shiki-light:#4078F2;--shiki-dark:#61AFEF;">    &quot;field2&quot;</span><span style="--shiki-light:#0184BC;--shiki-dark:#56B6C2;">:</span><span style="--shiki-light:#50A14F;--shiki-dark:#98C379;"> &quot;xxx&quot;</span></span>
<span class="line"><span style="--shiki-light:#383A42;--shiki-dark:#ABB2BF;">}</span></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>添加数据时，如果不指定文档 ID，ES 会自动分片一个随机 ID。这种情况下，结合 Hash 算法，可以保证数据被均匀分布到各个分片中。如果指定文档 ID，或指定 routing key，Hash 计算得出的值可能会不够随机，从而导致数据倾斜。</p><p><strong>index 一旦设置了主分片数就不能修改，如果要修改就需要 reindex（即数据迁移）</strong>。之所以如此，就是因为：一旦修改了主分片数，即等于修改了原 Hash 计算中的变量，无法再通过 Hash 计算正确路由到数据存储的分片。</p><h2 id="参考资料" tabindex="-1"><a class="header-anchor" href="#参考资料"><span>参考资料</span></a></h2><ul><li><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/index.html" target="_blank" rel="noopener noreferrer">Elasticsearch 官方文档</a></li><li><a href="https://www.itshujia.com/read/elasticsearch/359.html" target="_blank" rel="noopener noreferrer">Elasticsearch 从入门到实践之分布式文档的存储流程</a></li><li><a href="https://github.com/doocs/advanced-java/blob/main/docs/high-concurrency/es-write-query-search.md" target="_blank" rel="noopener noreferrer">https://github.com/doocs/advanced-java/blob/main/docs/high-concurrency/es-write-query-search.md</a></li><li><a href="https://www.elastic.co/blog/found-elasticsearch-top-down" target="_blank" rel="noopener noreferrer">https://www.elastic.co/blog/found-elasticsearch-top-down</a></li><li><a href="https://www.elastic.co/guide/en/elasticsearch/reference/current/preload-data-to-file-system-cache.html" target="_blank" rel="noopener noreferrer">https://www.elastic.co/guide/en/elasticsearch/reference/current/preload-data-to-file-system-cache.html</a></li><li><a href="https://blog.devgenius.io/elasticsearch-solution-to-searching-71116220c82f" target="_blank" rel="noopener noreferrer">https://blog.devgenius.io/elasticsearch-solution-to-searching-71116220c82f</a></li></ul>`,77)])])}const d=r(n,[["render",i]]),h=JSON.parse('{"path":"/pages/79ca17ad/","title":"Elasticsearch 架构","lang":"zh-CN","frontmatter":{"icon":"logos:elasticsearch","title":"Elasticsearch 架构","date":"2024-11-25T07:42:18.000Z","categories":["数据库","搜索引擎数据库","Elasticsearch"],"tags":["数据库","搜索引擎数据库","Elasticsearch","存储","索引"],"permalink":"/pages/79ca17ad/","description":"Elasticsearch 架构 存储流程 ES 存储数据的流程可以从三个角度来阐述： 从集群的角度来看，数据写入会先路由到主分片，在主分片上写入成功后，会并发写副本分片，最后响应给客户端。 从分片的角度来看，数据到达分片后需要对内容进行格式校验、分词处理然后再索引数据。 从节点的角度来看，ES 数据持久化的步骤可归纳为：Refresh、写 Trans...","head":[["script",{"type":"application/ld+json"},"{\\"@context\\":\\"https://schema.org\\",\\"@type\\":\\"Article\\",\\"headline\\":\\"Elasticsearch 架构\\",\\"image\\":[\\"https://raw.githubusercontent.com/dunwu/images/master/archive/2025/03/17e5ffbe2dad440d981823db4e9d4be5.png\\"],\\"datePublished\\":\\"2024-11-25T07:42:18.000Z\\",\\"dateModified\\":\\"2026-02-11T15:41:54.000Z\\",\\"author\\":[{\\"@type\\":\\"Person\\",\\"name\\":\\"钝悟\\",\\"url\\":\\"https://dunwu.github.io/waterdrop\\"}]}"],["meta",{"property":"og:url","content":"https://dunwu.github.io/waterdrop/waterdrop/pages/79ca17ad/"}],["meta",{"property":"og:site_name","content":"钝悟"}],["meta",{"property":"og:title","content":"Elasticsearch 架构"}],["meta",{"property":"og:description","content":"Elasticsearch 架构 存储流程 ES 存储数据的流程可以从三个角度来阐述： 从集群的角度来看，数据写入会先路由到主分片，在主分片上写入成功后，会并发写副本分片，最后响应给客户端。 从分片的角度来看，数据到达分片后需要对内容进行格式校验、分词处理然后再索引数据。 从节点的角度来看，ES 数据持久化的步骤可归纳为：Refresh、写 Trans..."}],["meta",{"property":"og:type","content":"article"}],["meta",{"property":"og:image","content":"https://raw.githubusercontent.com/dunwu/images/master/archive/2025/03/17e5ffbe2dad440d981823db4e9d4be5.png"}],["meta",{"property":"og:locale","content":"zh-CN"}],["meta",{"property":"og:updated_time","content":"2026-02-11T15:41:54.000Z"}],["meta",{"property":"article:tag","content":"索引"}],["meta",{"property":"article:tag","content":"存储"}],["meta",{"property":"article:tag","content":"Elasticsearch"}],["meta",{"property":"article:tag","content":"搜索引擎数据库"}],["meta",{"property":"article:tag","content":"数据库"}],["meta",{"property":"article:published_time","content":"2024-11-25T07:42:18.000Z"}],["meta",{"property":"article:modified_time","content":"2026-02-11T15:41:54.000Z"}]]},"git":{"createdTime":1732491963000,"updatedTime":1770824514000,"contributors":[{"name":"dunwu","username":"dunwu","email":"forbreak@163.com","commits":10,"url":"https://github.com/dunwu"}]},"readingTime":{"minutes":13.33,"words":3998},"filePathRelative":"12.数据库/搜索引擎数据库/Elasticsearch/Elasticsearch_架构.md","excerpt":"\\n<h2>存储流程</h2>\\n<p>ES 存储数据的流程可以从三个角度来阐述：</p>\\n<ul>\\n<li>从<strong>集群</strong>的角度来看，数据写入会先路由到主分片，在主分片上写入成功后，会并发写副本分片，最后响应给客户端。</li>\\n<li>从<strong>分片</strong>的角度来看，数据到达分片后需要对内容进行格式校验、分词处理然后再索引数据。</li>\\n<li>从<strong>节点</strong>的角度来看，ES 数据持久化的步骤可归纳为：<strong>Refresh、写 Translog、Flush、Merge。</strong></li>\\n</ul>\\n","autoDesc":true}');export{d as comp,h as data};
